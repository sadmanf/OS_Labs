To compile this program, enter the following into your command line:
	
	javac Pager.java

To run the program, enter the following:

	java Pager <machine size> <page size> <process size> <job mix> <references per process> <replacement algorithm>

There should be a total of 6 arguments, the first 5 being integers and the last being a string.